{
  "_comment": "vox: text-to-speech for spoken agent feedback (and voice input via vox_hear). FlexNetOS-built local stdio server. Requires `vox` on PATH. Merge into .claude/settings.json (Claude Code) or claude_desktop_config.json (Claude Desktop).",
  "mcpServers": {
    "vox": {
      "command": "vox",
      "args": ["serve"]
    }
  }
}
