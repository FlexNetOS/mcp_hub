{
  "_comment": "Sequential Thinking — official reference server: one tool for structured, revisable step-by-step reasoning. Uses `bunx` (not `npx`) because the meta-managed JS runtime is Bun and the bundled `npx` is non-functional. Merge into .claude/settings.json or claude_desktop_config.json.",
  "mcpServers": {
    "sequential-thinking": {
      "command": "bunx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]
    }
  }
}
